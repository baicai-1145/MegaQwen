"""MegaQwen utilities and optional ASR support.

This repo started as a single-model (Qwen3-0.6B) megakernel experiment.
The modules under `megaqwen/` provide small, reusable building blocks so we
can add new model variants (e.g. Qwen3-ASR) without rewriting scripts.
"""

